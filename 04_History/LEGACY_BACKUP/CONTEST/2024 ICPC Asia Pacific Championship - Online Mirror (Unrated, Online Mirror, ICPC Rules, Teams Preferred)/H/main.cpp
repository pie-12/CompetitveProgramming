//gah dang, imma pie12 【=◈︿◈=】
#include <bits/stdc++.h>
#define io freopen("main.inp","r",stdin); freopen("main.out","w",stdout);
#define fast ios::sync_with_stdio(false); cin.tie(nullptr);
#define int long long
#define float long double
#define endl '\n'
using namespace std;

signed main() {
    //io
    fast

    int n; cin >> n;
    int ans = 0;
    
    int hi_0, hi_1 = 0;
    int hi_0_pos, hi_1_pos;
    
    for(int i = 0; i < n; i++){
        string s; cin >> s;
        int cnt_1 = 0, cnt_0 = 0;
        for(int i = 0; i < s.size(); i++){
            if(s[i] == '0') cnt_0 ++;
            else cnt_1 ++;            
        }
        if(cnt_0 > )
        
    }
    
    cout << ans;
    return 0;
}