#include <iostream>
#include <set>
using namespace std;

int main()
{
    int m, n;
    cin >> m >> n;
    set<int>x;
    set<int>y;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
    {
        cin >> a[i][j];
        if (a[i][j]==1) {x.insert(i);y.insert(j);}
    }
    fill (a[0],b[0]+m*n,1);
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)

    return 0;
}
