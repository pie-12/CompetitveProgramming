#include <bits/stdc++.h>
using namespace std;



int main()
{
    long long n;
    cin >> n;
    long long o = 0;
    long long a[n], b[n], ;
    for (int i = 0; i < n; i++)
        cin >> a[i];
    for (int i = 0; i < n; i++)
        cin >> b[i];

    sort (a,a+n);
    sort (b,b+n);



}
